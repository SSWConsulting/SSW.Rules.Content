---
type: rule
title: Do you know how to get your data to your AI?
uri: get-data-to-your-ai
categories:
  - category: categories/artificial-intelligence/rules-to-better-ai-development.mdx
  - category: categories/artificial-intelligence/rules-to-better-ai.mdx
authors:
  - title: Isaac Lombard
    url: 'https://www.ssw.com.au/people/isaac-lombard/'
  - title: Jernej Kavka
    url: 'https://www.ssw.com.au/people/jernej-kavka/'
related:
  - rule: public/uploads/rules/ai-agent-memory-systems/rule.mdx
  - rule: public/uploads/rules/do-you-follow-best-practices-for-prompt-caching/rule.mdx
  - rule: public/uploads/rules/build-hallucination-proof-ai-assistants/rule.mdx
  - rule: public/uploads/rules/use-embeddings/rule.mdx
guid: ca33c8e4-dfdb-4903-bbe1-6c47bccdea91
seoDescription: 'Long context, vector retrieval (RAG) or agentic search - how to pick a data access strategy for your AI app, what each one costs you, and why RAG is not dead.'
created: 2026-07-30T00:00:00.000Z
createdBy: Isaac Lombard
createdByEmail: isaaclombard@ssw.com.au
lastUpdated: 2026-07-30T06:27:58.662Z
lastUpdatedBy: Isaac Lombard
lastUpdatedByEmail: isaaclombard@ssw.com.au
archivedreason: null
---

Your AI solution needs data: your codebase, your support docs, your business information.

How the model reaches it decides your cost per query, latency, and the quality of your response. RAG was the clear choice not too long ago, but now there's some nuance to the decision.

<endIntro />

<youtubeEmbed url="https://www.youtube.com/watch?v=UabBYexBD4k" description="Video: Is RAG Still Needed? Choosing the Best Approach for LLMs (11 min)" />

## Long context

The newest option. Context windows reaching up to and over a million tokens turned "just put it all in the prompt" into an intuitive, simple option.

That matters most where the answer isn't in any single document. A question like "*which security requirements never made it into the release?*" may need two or more documents in full to answer, because the answer is the gap between them.

It has caveats worth knowing. Positioning matters, and context in certain areas is predisposed to being missed – such as [in the middle](https://arxiv.org/abs/2307.03172) and in [multi-hop conversations](https://arxiv.org/abs/2601.12499).

### Making long context economical

Long context becomes even better when accounting for caching. Caching keys on how a prompt *starts*, not the whole thing. Put the documents at the front and the user's question at the end, and every request opening with those same documents reuses the cached copy at a fraction of the price, independent of the following question. Check the numbers for your own provider and model first - the minimum cacheable size, how long the cache lives and the size of the discount all differ. See [prompt caching](/do-you-follow-best-practices-for-prompt-caching).

That discount is what makes putting a whole corpus into every request affordable, so the ordering is an important design decision here.

## Retrieval-Augmented Generation (RAG)

RAG means retrieving the relevant material first and passing only that to the model. The retrieval itself can be keyword, structured, graph-based, vector similarity, or a mix; vector search over embeddings is the most common.

Best for large unstructured prose: support docs, PDFs, policies, transcripts. SSW's [RulesGPT](https://rulesgpt.ssw.com.au) works this way, and so does Cursor - it [chunks your repo and keeps the embeddings in a vector DB](https://cursor.com/blog/secure-codebase-indexing).

### "RAG is dead"

RAG as the **default pipeline** (i.e. chunk, embed, top-k, then stuff the prompt) is no longer an obvious choice, yet as a **capability**, it is still an important element of AI solution architecture. Claude Projects [switches RAG on](https://support.claude.com/en/articles/11473015-retrieval-augmented-generation-rag-for-projects) when a project outgrows the context window, then back off when it shrinks. And even the [best result for ditching the vector store](https://arxiv.org/abs/2602.23368) puts agentic search (see below) at roughly 90% of RAG's scores, not ahead of them.

## Agentic search

Hand the model `glob`, `grep` or a query API and let it look. There's no index to keep in sync, so nothing goes stale - as long as the tools read the live source and not a snapshot or a cache. Permissions can come from the access path too, but only if that path authenticates the caller and filters the results before the model ever sees them. [Claude Code dropped its vector store](https://www.latent.space/p/claude-code) for this.

That does not have to mean handing an agent a real shell. Vercel's [just-bash](https://github.com/vercel-labs/just-bash) is a simulated bash over a virtual filesystem - `grep`, `find`, `glob` and friends, no child processes, and network access off by default - so the model gets the search primitives without terminal access to the host.

The agent chooses its own search at query time, and can reach for any of the same tools RAG uses. Where it shines is **exact** matches. An exact hit that exists also cannot be ranked out of the results, the way top-k similarity search risks quietly dropping it.

The flip side is that the model has to guess the search term. Ask about "retry logic" when the code says `circuit_breaker` and the first search returns nothing, so the agent tries again with different words. That recovery is why the cost lands as latency and tokens rather than a wrong answer… and why questions phrased in genuinely different vocabulary to the source still favour embeddings.

## So which should I use?

These are composable building blocks, not mutually exclusive architectures - retrieval can select the relevant material before the model reasons over it in context.

That said, three questions settle most cases: whether the corpus fits with room to spare, whether the answer needs all of it at once, and whether the same documents come back on every request.

<imageEmbed alt="Decision tree for choosing a data access strategy" size="large" showBorder={false} figurePrefix="none" figure="Choosing a data access strategy by corpus size, reasoning scope, and whether the documents repeat" src="/uploads/rules/get-data-to-your-ai/data-access-decision-tree-v3.svg" />

<boxEmbed
  style="greybox"
  body={<>
    [YakShaver](https://yakshaver.ai) turns a screen recording into a PBI, which means picking the right backlog out of every project a client has.
    
    Walk the decision tree above: the list of projects and what each one covers is small, so it **fits with room to spare**. And picking the best match means weighing every project against the others, not retrieving the three that look closest - so it **needs the whole corpus at once**.
    
    Two questions, and it lands on long context. The project list is also identical on every request, so it caches cleanly.
  </>}
  figurePrefix="good"
  figure="YakShaver needs to compare every project to route a PBI, so the whole list goes in the prompt"
/>

That gets you to a starting point. Check it against the detail below - particularly what drives the cost and how each option fails, since those are what you live with once it is built.

Hybrid solutions and more complex context engineering are also options if you need to further optimise the solution.

|                    | Long context                                                                                 | RAG (retrieval)                                                              | Agentic search                                               |
| ------------------ | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------ |
| **Corpus size**    | Must fit with room to spare                                                                  | Bigger than the window - bounded by index and retrieval limits               | Bigger than the window - bounded by what each search returns |
| **Best for**       | Whole-corpus reasoning - comparisons, omissions, contradictions,&#xA;**single-turn queries** | Large prose corpora needing citable answers                                  | Exact identifiers, live or permission-sensitive data         |
| **Infrastructure** | None                                                                                         | A retrieval layer to build and keep in sync                                  | A sandboxed shell or query API                               |
| **Cost driver**    | Re-sent every request - caching only helps if the documents repeat                           | Indexing up front, re-indexing as documents change, plus retrieval per query | Tokens and turns while the agent searches                    |
| **Fails by**       | Losing the middle of a long window                                                           | Silent failure - the answer was there, retrieval missed it                   | Missing a match worded differently to the source             |
