---
type: rule
title: Do you use only restrict write operations to Safe Outputs in your GitHub Agentic workflows?
uri: github-agentic-workflows-use-safe-outputs
categories:
  - category: categories/artificial-intelligence/rules-to-better-ai-assisted-development.mdx
authors:
  - title: Caleb Williams
    url: 'https://www.ssw.com.au/people/caleb-williams'
related:
  - {}
  - {}
  - {}
guid: 7c431def-6fbc-4296-8769-d9d4d082d2ad
seoDescription: 'Learn why GitHub agentic workflows should use structured safe outputs instead of natural language instructions or MCP scripts for reliable, idiomatic automation.'
created: 2026-03-13T00:00:00.000Z
createdBy: Caleb Williams
createdByEmail: CalebWilliams@ssw.com.au
lastUpdated: 2026-03-13T03:34:45.041Z
lastUpdatedBy: Caleb Williams
lastUpdatedByEmail: CalebWilliams@ssw.com.au
---

When building GitHub agentic workflows, it is tempting to have the agent communicate decisions and actions through natural language, writing out instructions in prose or calling external tooling via MCP scripts. However, this approach is fragile, hard to test, and difficult to maintain. There's ultimately no guarantee the agent will execute the code you expect it to.

The reliable and safe approach is to structured outputs, deterministic data emitted by the agent that downstream workflow steps can reliably parse and act upon.

<endIntro />

## What are safe outputs?

Safe outputs are structured values produced by an agent step. typically JSON, YAML, or key-value pairs written to `$GITHUB_OUTPUT` or a job summary, that subsequent steps consume directly. They are:

* **Controllable: **The same input produces the same output format
* **Parseable:** Downstream steps use `fromJSON()` or step output references, not string matching on natural language
* **Auditable:** Clearly visible in workflow logs and job summaries
* **Secure:** Using safe outputs for tool calls avoids giving the agent access to potentially dangerous secrets in your repository.

## Why not use MCP scripts?

MCP scripts are very useful for reliable read operations on your repo, for example fetching data from an external API that requires authentication. repeatable instructions to your agent, but they are not recommended for write operations in GitHub Agentic Workflows. The reason for this is they bypass the audit trail that runs implicitly whenever your agent runs the alternatives are:

If you need to clean up after your workflow, or synchronise with an external system you also have the option of using post steps , but these aren't designed to be used by your Agent.

<boxEmbed
  body={<>
    If your workflow needs an MCP to write to an external system, you probably need a Custom Safe Output. See [documentation](https://github.github.com/gh-aw/reference/custom-safe-outputs/).
  </>}
  figurePrefix="none"
  figure=""
  style="info"
/>

## Examples

````yaml
---
description: >
  Create an example an issue for adding telemetry to the site.

on:
  workflow_dispatch:

engine:
  id: copilot
  model: gpt-5-mini

permissions: read-all

---
I want you to create an issue in this repository for adding telemetry to the site. 

Use the command below to create an issue via the GitHub REST API:

```bash
curl -s -X POST \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  -H "X-GitHub-Api-Version: 2022-11-28" \
  "https://api.github.com/repos/${{ github.repository }}/issues" \
  -d '{
    "title": "<TITLE>",
    "body": "<BODY>"
  }'
```

Where the agent fills in:
- `<TITLE>` — a concise issue title describing the request
- `<BODY>` — the full issue body in markdown (JSON-escaped)

````

**Figure: ❌ Bad example - creating GitHub issues using curl in the body of a flow**

```yaml
---
description: >
  Create an example issue for adding telemetry to the site using
  safe-outputs instead of curl.

on:
  workflow_dispatch:


permissions: read-all

safe-outputs:
  create-issue:
    title-prefix: "[Example] "
    max: 1

tools:
  github:
    lockdown: false
    toolsets: [issues]
---
I want you to create an issue in this repository for adding telemetry to the site.

Use the `create_issue` safe-output tool to create the issue.

- `<TITLE>` — a concise issue title describing the request
- `<BODY>` — the full issue body in markdown
```

**Figure: ✅ Good example - Defining Issue creation as a safe output and using the built in toolset**

## Key principles

1. **Emit structured data, not prose:** use `$GITHUB_OUTPUT`, JSON files, or job summaries
2. **Avoid MCP calls for workflow control:** MCP is great for giving agents *context*, not for *driving* workflow steps
3. **Reference outputs explicitly:** use `steps.<id>.outputs.<key>` so the relationship is visible in the workflow definition
4. **Keep agent logic separate from workflow logic:** the agent decides, the workflow acts
