---
archivedreason: null
authors:
- title: Adam Cogan
  url: https://ssw.com.au/people/adam-cogan
created: 2018-04-30 21:21:26+00:00
createdBy: TiagoAraujo
createdByEmail: TiagoAraujo@ssw.com.au
guid: 30ea0a57-c66d-44f4-87b6-61ae5ee8972a
isArchived: false
lastUpdated: 2023-08-17 00:59:01+00:00
lastUpdatedBy: Brook Jeynes [SSW]
lastUpdatedByEmail: jeynesbrook@gmail.com
redirects:
- do-you-use-a-helper-extension-method-to-raise-events
seoDescription: Use a helper extension method to simplify event raising and reduce
  code maintenance complexity.
title: Do you use a helper extension method to raise events?
type: rule
uri: use-a-helper-extension-method-to-raise-events
---

<introEmbed
  body={<>
Enter Intro Text
  </>}
/>
Instead of:

```csharp
private void RaiseUpdateOnExistingLotReceived() {
  if (ExistingLotUpdated != null) {
    ExistingLotUpdated();
  }
}
```

...use this event extension method:

```csharp
public static void Raise<t>(
  this EventHandler<t> @event,
  object sender,
  T args
) where T : EventArgs {
  var temp = @event;

  if (temp != null) {
    temp(sender, args);
  }
}

public static void Raise(this Action @event) {
  var temp = @event;

  if (temp != null) {
    temp();
  }
}
```

That means that instead of calling:

```csharp
RaiseExistingLotUpdated();
```

...you can do:

```csharp
ExistingLotUpdated.Raise();
```

Less code = less code to maintain = less code to be blamed for ;)