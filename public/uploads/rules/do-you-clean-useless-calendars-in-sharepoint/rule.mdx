---
archivedreason: null
authors:
- title: William Yin
  url: https://ssw.com.au/people/william-yin
- title: Brendan Richards
  url: https://ssw.com.au/people/brendan-richards
created: 2013-09-06 00:00:25+00:00
createdBy: System Account
createdByEmail: Unknown@ssw.com.au
guid: 1e12b128-b34c-4435-af21-81bdef31f061
isArchived: false
lastUpdated: 2015-12-09 23:13:14+00:00
lastUpdatedBy: Ulysses Maclaren
lastUpdatedByEmail: ulyssesmaclaren@ssw.com.au
redirects: []
seoDescription: SharePoint calendar cleanup simplifies site organization by removing
  unused event lists and streamlining site maintenance.
title: Do you clean useless calendars in SharePoint?
categories:
  - category: categories/infrastructure-and-networking/rules-to-better-sharepoint.mdx
type: rule
uri: do-you-clean-useless-calendars-in-sharepoint
---

Most SharePoint site templates contain a calendar list, this will bring lots of useless calendars.

<endIntro />

Use the below PowerShell script to clean them:

```pwsh
$site = Get-SPSite("http://<site collection URL>/"); # Specify url here
foreach ($web in $site.AllWebs) {
    $lists = $web.Lists
    for ($i=($lists.Count-1);$i -gt 0; $i--) {
        $list = $lists[$i]
        #Write-host $i  $list.Title $list.BaseTemplate.ToString()
        if ($list.BaseTemplate.ToString().ToLower().contains('events')) {
            if ($list.Items.Count -eq 0)
            {
                Write-Host $list.Items.Count "items in the list" $list.Title '('$list.BaseTemplate') at '$web.Url "- cleaning it!"
                $list.Recycle()
                #$list.Delete()
            }
        }
    }
}
```

This script will put the calendars which do not have any events into **Site Settings** | **Recycle Bin**:

<imageEmbed
  alt="Image"
  size="large"
  showBorder={false}
  figurePrefix="none"
  figure="Empty Calendars in Recycle Bin folder"
  src="/uploads/rules/do-you-clean-useless-calendars-in-sharepoint/EmptyCalendarsInRecyckeBin.png"
/>