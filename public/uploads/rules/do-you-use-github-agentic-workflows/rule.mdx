---
title: Do you use GitHub Agentic Workflows?
uri: do-you-use-github-agentic-workflows
categories:
  - category: categories/artificial-intelligence/rules-to-better-ai-assisted-development.mdx
  - category: categories/artificial-intelligence/rules-to-better-ai.mdx
authors:
  - title: Michael Qiu
    url: 'https://ssw.com.au/people/michael-qiu'
  - title: Eddie Kranz
    url: 'https://ssw.com.au/people/eddie-kranz'
related:
  - rule: public/uploads/rules/workflow-naming-scheme/rule.mdx
guid: e33d6ccb-c24c-4f1b-bcd6-7af29f2caa1a
seoDescription: 'Learn how to use GitHub Agentic Workflows to automate repository tasks like issue triage, documentation updates, and test generation using AI coding agents.'
created: 2026-02-24T04:24:59.348Z
createdBy: Michael Qiu
createdByEmail: MichaelQiu@ssw.com.au
lastUpdated: 2026-02-24T06:31:23.911Z
lastUpdatedBy: Michael Qiu
lastUpdatedByEmail: MichaelQiu@ssw.com.au
---

GitHub Agentic Workflows let you automate repository tasks by writing natural language instructions in markdown. These markdown files then compile into GitHub Actions workflows powered by AI agents. Instead of writing complex YAML, you describe what you want done and an AI agent handles it.

<endIntro />

## What are GitHub Agentic Workflows?

GitHub Agentic Workflows are a new automation layer built on top of GitHub Actions. You write a markdown file with YAML frontmatter describing **triggers**, **permissions**, **safe outputs**, and **tools**, followed by natural language instructions. The `gh aw` CLI extension then compiles this into a lock file, which is similar to a GitHub Actions YAML file.

The key difference from traditional CI/CD is that instead of scripting every step, you describe the desired behavior in plain English and an AI agent (such as GitHub Copilot, Claude Code, or OpenAI Codex) interprets and executes it.

A workflow file has four key parts:

* **Triggers** (`on:`) - Standard GitHub Actions events (push, schedule, issue opened, etc.)
* **Permissions** (`permissions:`) - Scoped access controls, read-only by default
* **Safe outputs** (`safe-outputs:`) - A way for the agent to delegate potentially dangerous tasks safely (e.g. creating a PR or issue, writing a comment, etc.)
* **Tools** (`tools:`) - Operations the agent can use (e.g. edit, web-fetch)

The instructions section is plain markdown where you describe the agent's role, goals, and steps.

<boxEmbed
  body={<>
    Agentic Workflows compile to standard GitHub Actions YAML. This means they are repo-centric, team-visible, and auditable - no hidden prompts or secret behavior. You can inspect the generated `.lock.yml` file to see exactly what will execute.
  </>}
  figurePrefix="none"
  figure=""
  style="info"
/>

{{ TODO: WRITE A SECTION ON HOW IT DIFFERS FROM JUST GETTING AI TO WRITE A WORKFLOW }}

<boxEmbed
  body={<>
    **Security by design:** Write operations require explicit approval via `safe-outputs`. The agent runs in a sandboxed container with minimal permissions, tool allowlisting, and network isolation.
  </>}
  figurePrefix="none"
  figure=""
  style="warning"
/>

## When should you use GitHub Agentic Workflows?

Agentic Workflows are best suited for repetitive repository maintenance tasks that benefit from AI reasoning, and do not require strict, deterministic outputs. They particularly shine for open-source maintainers dealing with high-volume repositories.

Common use cases include:

* **Issue triage** - Automatically label, summarize, and route incoming issues
* **Documentation updates** - Keep READMEs and API docs in sync as code evolves
* **Test generation** - Analyze coverage gaps and propose new test cases via PRs
* **Daily reporting** - Generate status reports summarizing recent activity
* **Code quality** - Suggest refactoring improvements and file PRs with fixes

Here's an example workflow that marks and closes stale issues using a two-strike warning system:

{{ TODO: REPLACE THIS WITH THE DUPLICATED ISSUES MD }}

```markdown
---
description: >
  Weekly stale issue checker. Scans open issues for 6+ months of inactivity,
  pings assignee/author, uses a two-strike approach before closing.
on:
  schedule:
    - cron: "0 8 * * 1"
  workflow_dispatch:
timeout-minutes: 15
engine: codex
permissions:
  issues: read
  contents: read
tools:
  github:
    toolsets: [issues, labels]
safe-outputs:
  add-comment:
    max: 15
    target: "*"
  add-labels:
    max: 15
    allowed: [awaiting-response, stale]
  remove-labels:
    max: 15
    allowed: [awaiting-response, stale]
  close-issue:
    max: 10
    required-labels: [stale]
---

# Stale Issue Checker

You are a repository maintenance assistant for `${{ github.repository }}`.
Your job is to identify stale issues and follow a structured two-strike
process before closing them.

## Definitions

- **Stale threshold**: No activity for 6 months (180 days).
- **Warning threshold**: Issue has the `awaiting-response` label AND no
  activity for 14 days since that label was added.
- **Exempt labels**: Issues with `critical`, `security`, or `blocked`
  must NEVER be pinged, warned, or closed.

## Process

1. Gather all open issues with their metadata
2. Classify each issue:
   - **Close** — already has `stale` label, no response for 14+ days
   - **Closing warning** — has `awaiting-response`, no response for 14+ days
   - **First ping** — no stale labels, inactive for 180+ days
   - **Recovered** — has stale labels but recent activity; clean up labels
3. Take action per category (contextual comments, label changes, closes)
4. Output a summary of all actions taken

## Important rules

- NEVER count the bot's own comments as "activity"
- Be contextual in comments — read the issue before writing
- Respect the two-strike model strictly
```

## How do you set up GitHub Agentic Workflows?

### Option 1: Agentic - Vibe it up! 🤙

1. Prompt your AI Agent to write you an Agentic Workflow with the following prompt:

```markdown
Create a workflow for GitHub Agentic Workflows using https://raw.githubusercontent.com/github/gh-aw/main/create.md

{{ YOUR WORKFLOW SPECIFICATION HERE }}
```

### Option 2: Handcraft your prompt 🤓

If you prefer to manually create your Agentic Workflows, follow these instructions. Learn more at [GitHub Agentic Workflow Docs - Manual Editing](https://github.github.com/gh-aw/setup/creating-workflows/#manual-editing)

1. Install the `gh aw` GitHub CLI extension
2. Write a `.md` file with YAML frontmatter (triggers, permissions, safe-outputs, tools) followed by natural language instructions
3. Run `gh aw` to generate the GitHub Actions `.lock.yml` file from your markdown
4. Add both the `.md` source and the generated `.lock.yml` to your repository
5. The workflow runs on the events you specified, with the AI agent executing your instructions

<boxEmbed
  body={<>
    You can also create workflows directly through the GitHub web interface using natural language prompts, without needing the CLI.
  </>}
  figurePrefix="none"
  figure=""
  style="tips"
/>

<boxEmbed
  body={<>
    GitHub Agentic Workflows is currently a research project from GitHub Next and Microsoft Research. Specifications may change, and careful human supervision is recommended.
  </>}
  figurePrefix="none"
  figure=""
  style="warning"
/>

***

## Additional reading

* [GitHub Next - Agentic Workflows](https://githubnext.com/projects/agentic-workflows/) - The introductory article explaining the vision and design philosophy
* [gh-aw Documentation](https://github.github.com/gh-aw/) - Official documentation for the CLI extension and workflow format
* [Example Workflows](https://github.com/githubnext/agentics) - A collection of demonstrator agentic workflows
