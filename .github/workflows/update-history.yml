# Update the hisoty.json file with created and modified dates for added/updated rules

name: Update-History

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main, content-migration-14 ]
  pull_request:
    branches: [ main, content-migration-14 ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      - uses: lots0logs/gh-action-get-changed-files@2.1.4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: print files added
        run: cat ${HOME}/files_added.json

      - name: print files modified
        run: cat ${HOME}/files_modified.json

      - name: remove changedFiles json
        run: |
          rm ${HOME}/files.json
          rm ${HOME}/files_modified.json
          rm ${HOME}/files_added.json
          rm ${HOME}/files_deleted.json
          rm ${HOME}/files_renamed.json
      

